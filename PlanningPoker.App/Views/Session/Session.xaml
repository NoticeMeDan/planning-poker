<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PlanningPoker.App.Views.Session.Session"
    Title="Title"
>
    <StackLayout>
        <Label Text="Item title"/>

        <ListView
             ItemsSource="{Binding Players}"
             VerticalOptions="FillAndExpand"
             HasUnevenRows="true"
             IsPullToRefreshEnabled="true"
             RefreshCommand="{Binding LoadPlayersCommand}"
             IsRefreshing="{Binding IsBusy, Mode=OneWay}"
             SelectionMode="None">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Nickname}"
                                   LineBreakMode="NoWrap" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--<ListView
            ItemsSource="{Binding Votes}"
            VerticalOptions="FillAndExpand"
            HasUnevenRows="true"
            IsPullToRefreshEnabled="true"
            RefreshCommand="{Binding LoadEstimateVotesCommand}"
            IsRefreshing="{Binding IsBusy, Mode=OneWay}"
            SelectionMode="None">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Estimate}"
                                   LineBreakMode="NoWrap" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->

        <Grid
            VerticalOptions="End"
            Margin="20"
            ColumnSpacing="5"
            >
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="55" />
                <ColumnDefinition Width="55" />
                <ColumnDefinition Width="55" />
                <ColumnDefinition Width="55" />
                <ColumnDefinition Width="55" />
                <ColumnDefinition Width="55" />
            </Grid.ColumnDefinitions>

            <Button Text="1" Grid.Row="0" Grid.Column="0" Clicked="OnVoteCard_Clicked" />
            <Button Text="3" Grid.Row="0" Grid.Column="1" Clicked="OnVoteCard_Clicked" />
            <Button Text="5" Grid.Row="0" Grid.Column="2" Clicked="OnVoteCard_Clicked" />
            <Button Text="8" Grid.Row="0" Grid.Column="3" Clicked="OnVoteCard_Clicked" />
            <Button Text="13" Grid.Row="0" Grid.Column="4" Clicked="OnVoteCard_Clicked" />
            <Button Text="20" Grid.Row="0" Grid.Column="5" Clicked="OnVoteCard_Clicked" />
        </Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Text="Play Nitpicker" Grid.Row="0" Grid.Column="0" Clicked="OnNitpicker_Clicked" />
            <Button Text="Re-vote" Grid.Row="0" Grid.Column="1" Clicked="OnReVote_Clicked" />
            <Button Text="Next Item" Grid.Row="0" Grid.Column="2" Clicked="OnNextItem_Clicked" />
        </Grid>

    </StackLayout>
</ContentPage>
